// makeavailable to other files
export const area = (width:number, length:number) =>{
    return width * length
}

export const perimeter = (width:number, length: number) =>{
    return (width*2 )+ (length*2)
}